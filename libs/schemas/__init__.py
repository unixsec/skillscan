"""Cross-layer Pydantic DTOs for untrusted-input validation at trust boundaries
(coding spec repo layout: `libs/schemas/`). The only layer in this codebase
where Pydantic validates security-relevant data - see `findings.py`."""
